# Enum(Dev, Prod)
MODE="dev"
PROJECT_NAME="full-api"
APP_TITLE="Full API"
APP_DESCRIPTION="풀스택 템플릿 API"
APP_VERSION="1.0.0"

# CORS 설정: ,로 구분해서 작성(주의. 띄어쓰기 없어야 함)
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
ALLOWED_HOSTS="*"

# Docker-compose 설정
WEB_PORT=8000
DOCKER_PORT=8000
BACKEND_PORT=8000
FRONTEND_PORT=3000
DEBUG_PORT=5678
FRONTEND_SSL_PORT=443

# Gunicorn 설정
TIMEOUT=120
KEEP_ALIVE=5
WORKERS=1

# PostgreSQL DB 설정
POSTGRES_USER=postgre_dev_user
POSTGRES_PASSWORD=postgre_dev_password_123
POSTGRES_DB=postgre_dev_db
POSTGRES_HOST=postgre_dev_host
POSTGRES_PORT=5432

# Redis DB 설정
REDIS_HOST="redis"
REDIS_PORT=6379

# 백엔드 설정
APP_ENV=development
DEBUG=true
LOG_LEVEL=debug
SECRET_KEY=dev-secret-key-for-development-only-do-not-use-in-production
API_V1_STR=/api/v1
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# 어드민 초기 계정 정보
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=admin

# 프론트엔드 설정
VITE_API_URL=http://localhost:8000
NODE_ENV=development

# pgAdmin 설정 (옵션 - docker-compose profiles=tools 사용 시)
PGADMIN_EMAIL=admin@nara.local
PGADMIN_PASSWORD=admin
